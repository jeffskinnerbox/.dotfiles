
-- provides a better UI than the the command prompt for some data entry (a popup picklist or window near the point of data entry)

return {
  "stevearc/dressing.nvim",
  event = 'VeryLazy',                                                           -- loading will be done after 'LazyDone' event and 'VimEnter' auto-commands
}
